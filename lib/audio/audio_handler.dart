/// AudioService handler — xem [audio.md].
///
/// TODO(M2): extends BaseAudioHandler, kết nối AudioControllerImpl
class AppAudioHandlerPlaceholder {
  static Future<void> init() async {
    // AudioService.init(builder: () => MyAudioHandler());
  }
}
