/// Tên bảng / cột SQLite — xem [constants.md], [KIEN_TRUC.md].
abstract final class DbConstants {
  static const dbName = 'local_music_player.db';
  static const dbVersion = 1;

  static const tracksTable = 'tracks';
}
