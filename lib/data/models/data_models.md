# data models — Entity / DTO (tầng data)

## Mục đích

Map `Map<String, dynamic>` từ SQLite ↔ `domain/models`.

## Việc cần làm

- [ ] `track_entity.dart` — `fromMap` / `toMap` / `toDomain()`
- [ ] Tương tự playlist, history nếu cần tách khỏi DAO

## Phase

M1
